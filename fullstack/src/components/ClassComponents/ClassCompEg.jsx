import React from "react";
class ClassCompEg extends React.Component{
       render()
       {
        return(
            <div>
                <h2>This is Class Component Example</h2>
            </div>
        )
       }
}
export default ClassCompEg

//A default export in JavaScript is a way to export one primary entity (function, class, or object) from a module. It’s commonly used when a file contains one main purpose or functionality, such as a React component.